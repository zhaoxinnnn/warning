const initState = 3939339;
export default (state = initState, action) => {
    switch (action.type) {
        case "GETERRORINFOS" :
            return state;
        default :
            return state;
    }
}