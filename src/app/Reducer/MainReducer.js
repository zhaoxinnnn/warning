const initState = 0;
export default (state = initState, action) => {
    switch (action.type) {
        case "GETERRORINFOS" :
            return state;
        default :
            return state;
    }
}